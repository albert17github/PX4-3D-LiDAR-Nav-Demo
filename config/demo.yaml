frame_id: map
start: [0.0, 0.0, 2.2]
goal: [7.0, 7.0, 2.2]
# Simulation-only acceptance geometry. DLIO's local map axes inherit the
# vehicle's initial heading, so world coordinates are transformed before they
# are compared with planner points in map.
simulation_map_origin_world_xy_yaw_rad: [-8.0, -6.0, 0.55]
simulation_vehicle_model_name: x500_lidar_3d
expected_obstacle_center_world_xy: [-6.845569474049038, -1.186758871534423]
expected_obstacle_model_name: cyan_pillar
expected_obstacle_radius_m: 0.65
minimum_demo_clearance_m: 0.70
minimum_cross_track_m: 0.55
takeoff_height_m: 2.2
cruise_speed_mps: 0.65
setpoint_rate_hz: 20.0
waypoint_tolerance_m: 0.30
goal_hold_s: 3.0
map_warmup_s: 12.0
planner_attempts: 5
planner_retry_interval_s: 4.0
mission_timeout_s: 150.0
planner_service: /demo/minimal_planner/get_path
interactive_goal_topic: /demo/goal_pose
interactive_goal_min_distance_m: 1.0
interactive_goal_max_distance_m: 12.0
interactive_goal_timeout_s: 1800.0
